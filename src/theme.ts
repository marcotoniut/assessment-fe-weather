export const spacing = {
  small: 7,
  default: 10,
} as const;

export const fontSizes = {
  default: '1rem',
  large: '1.25rem',
} as const;

export const boxSize = 400;

export const secondaryColor = '#666';
